# Frontend — React app (Phase 3)

A simple React UI: an applicant form that POSTs to the backend's /credit-risk
endpoint and renders the risk score, category, approval, and reasons.
Scaffold with Vite when Phase 2's API is live.

